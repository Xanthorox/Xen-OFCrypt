<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net9.0-windows</TargetFramework>
    <UseWPF>true</UseWPF>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <NoWarn>MSB3277</NoWarn>
  </PropertyGroup>

  <!-- Reference the builder DLL directly (no WPF needed — we only use Core namespace) -->
  <ItemGroup>
    <Reference Include="XanthoroxCrypted">
      <HintPath>..\Bin\Builder\XanthoroxCrypted.dll</HintPath>
    </Reference>
  </ItemGroup>

</Project>
